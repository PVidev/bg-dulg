/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vercel автоматично оптимизира Next.js проектите
  // data/ папката ще бъде включена автоматично в build
}

module.exports = nextConfig

