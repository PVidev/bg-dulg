# UPDATE-V4: План за пускане на публично API и абонамент

Цел: EUSpam да предложи **публично API** за разработчици, приложения, оператори и партньори, с ясни **планове и цени** и възможност за **абонамент**.

**Статус:** 📋 план (за изпълнение)

---

## 1. Какво ни трябва общо

| Област | Нужно |
|--------|--------|
| **API слой** | REST endpoint за проверка (тип + стойност) + опционално други (статистики, списък). Автентикация с API ключ. |
| **Управление на ключове** | Таблица за API ключове, свързани с акаунт/план; генериране, отзоваване, лимити по план. |
| **Планове и лимити** | Дефиниране на планове (безплатен / старт / бизнес / корпоратив), лимит заявки/ден или/месец. |
| **Абонамент и плащане** | Решение за събиране на пари: разплащане (Stripe, PayPal, банков превод) и/или само регистрация + ръчно одобрение за платени планове. |
| **Портал за разработчици** | Страница „За разработчици“ / „API“ с документация, цени, регистрация за ключ, линк към условия за използване на API. |
| **Правни документи** | Условия за ползване на API (лимити, забранено злоупотреба, отговорност). |

---

## 2. API дизайн – какво да предлагаме

### 2.1 Основен endpoint (задължителен за пускане)

- **Провери стойност**  
  - `GET /api/v1/check?type=phone|website|email|...&value=...`  
  - Заявка с **API ключ** в заглавка: `X-API-Key: <key>` или `Authorization: Bearer <key>`.  
  - Отговор: дали има одобрени сигнали за тази стойност, брой (опционално), обобщен риск/категория (по избор).  
  - Това е същото като логиката на `search.php`, но с ключ и лимит по план.

### 2.2 Опционални endpoints (по фази)

- **Статистики (агрегирани)** – брой одобрени сигнали, по тип/категория (вече има `stats.php`; да се обвие с API ключ и лимит).
- **Списък последни** – последни N одобрени записа с маскирани стойности (подобно на `list.php`).
- **Webhook** (по-късно) – при нов одобрен сигнал за дадена стойност да се изпраща нотификация към URL на абоната.

### 2.3 Автентикация и лимити

- Всяка заявка към публичното API изисква валиден **API ключ**.
- Ключът е свързан с **план** (free / start / business / enterprise).
- По план се прилага **rate limit**: напр. X заявки/ден или X заявки/месец; при надвишаване → 429 Too Many Requests.
- Логиране на използване (по ключ, дата) за отчети и биллинг (ако има платени планове).

---

## 3. Планове и цени – опции

### Вариант A: Прост старт (без автоматично плащане)

| План | Лимит | Цена | За кого |
|------|--------|------|--------|
| **Безплатен** | 100 заявки/ден | 0 лв. | Тестване, малки проекти |
| **Старт** | 1 000 заявки/ден | по договаряне / месец | Малки приложения, сайтове |
| **Бизнес** | 10 000 заявки/ден | по договаряне / месец | Оператори, по-големи интеграции |
| **Корпоратив** | по договаряне | по договаряне | Банки, големи партньори |

- Регистрация за ключ през форма на сайта; за платени планове – **ръчен контакт** (имейл/форма), договор и превод. Вие активирате плана и повишавате лимита в админката.

### Вариант B: Самостоятелен абонамент (с разплащане)

| План | Лимит | Цена (пример) | Плащане |
|------|--------|----------------|--------|
| **Free** | 100 заявки/ден | 0 лв. | — |
| **Starter** | 1 000 заявки/ден | 29 лв./мес. | Stripe / PayPal |
| **Business** | 10 000 заявки/ден | 99 лв./мес. | Stripe / PayPal |
| **Enterprise** | индивидуален лимит | по оферта | Договор + превод |

- Регистрация за API ключ + избор на план; за платени – месечен абонамент с карта. Нужен е **интегратор за плащане** (виж по-долу).

### Вариант C: Хибрид

- **Безплатен план** – само регистрация, автоматично получаване на ключ и лимит.
- **Платени планове** – първо контакт (форма/имейл), после вие изпращате оферта; плащане по банков път или с добавяне на Stripe/PayPal по-късно.

---

## 4. Какво ми е нужно технически

### 4.1 База данни

- **Таблица `api_keys`** (или `api_credentials`):  
  - `id`, `key_hash` (хеш на ключа, не самият ключ), `name` (име на приложение/компания), `contact_email`, `plan` (free|start|business|enterprise), `requests_per_day` или `requests_per_month`, `is_active`, `created_at`, `expires_at` (по избор).
- **Таблица `api_usage`** (по избор за биллинг):  
  - `api_key_id`, `date`, `request_count` – за проследяване на потребление.

### 4.2 Бекенд (PHP)

- **Генериране на API ключ** – при регистрация да се генерира уникален ключ (напр. `euspam_` + random string), да се съхрани само хешът в БД; самият ключ се показва веднъж на потребителя.
- **Middleware/проверка** – при всяка заявка към `/api/v1/*`: извличане на ключ от заглавка, търсене по хеш, проверка дали е активен и дали лимитът не е надвишен; при успех – изпълнение на endpoint-а.
- **Rate limit по ключ** – броене на заявки за текущия ден/месец за този ключ; при надвишаване връщане на 429 и съобщение за лимит.

### 4.3 Портал (фронтенд)

- **Страница „API“ или „За разработчици“** (`/developers` или `/api-docs`):  
  - Описание какво прави API-то.  
  - Документация: endpoint, параметри, примерни заявки (curl), примерен отговор.  
  - Таблица с планове и цени.  
  - Форма за **регистрация за API ключ**: имейл, име на приложение/компания, избор на план (напр. само Free за начало).  
  - След регистрация – показване на ключа веднъж и препоръка да се запази на сигурно място.
- **Личен кабинет за разработчици** (по избор):  
  - Вход с имейл/парола или магически линк.  
  - Преглед на ключове, потребление (заявки/ден), смяна на план, отзоваване на ключ.

### 4.4 Плащане (ако изберете абонамент с карта)

- **Stripe** или **PayPal** (или и двете):  
  - Регистрация в Stripe/PayPal, получаване на API ключове.  
  - При избор на платен план – създаване на абонамент (subscription); месечно таксуване.  
  - Webhook от Stripe/PayPal при плащане/отказ – обновяване на плана в нашата БД (увеличен лимит, срок на абонамента).
- **Алтернатива без автоматизация:** само банков превод + ръчно активиране на план в админ панела.

---

## 5. Стъпки за пускане (порядок)

### Фаза 1: Минимално пускане (без плащане)

1. Дефиниране на **един публичен endpoint** – проверка (тип + стойност), със същата логика като `search.php`.  
2. Въвеждане на таблица **`api_keys`** и логика за проверка на ключ + лимит (напр. 100 заявки/ден за всички в началото).  
3. Страница **„За разработчици“** с кратка документация и форма за **заявка за API ключ** (имейл, име на приложение); вие ръчно генерирате ключ и изпращате по имейл (или показвате в проста „благодарна“ страница след форма).  
4. Обновяване на **Условия за ползване** – раздел за използване на API (лимити, забрана за злоупотреба, „as is“).

### Фаза 2: Планове и автоматичен ключ

1. Добавяне на **планове** в БД (free / start / business) с полета за лимит (заявки/ден или/месец).  
2. При регистрация за ключ – избор на план; автоматично генериране на ключ и показване веднъж.  
3. В админ панела – преглед на API ключове и потребление (ако имате `api_usage`).

### Фаза 3: Абонамент и плащане (по избор)

1. Интеграция с **Stripe** (или PayPal): създаване на продукти/цени за месечен абонамент.  
2. В портала за разработчици – бутон „Премини към платен план“ → Stripe Checkout; след успешно плащане – webhook обновява плана в БД.  
3. Опционално: имейл нотификации при достигане на 80% от лимита или при неуспешно плащане.

---

## 6. Примерни цени (за ориентир)

- Цените са **примерни**; можете да ги коригирате според пазара и разходите.

| План       | Заявки/ден | Цена/мес. (пример) |
|-----------|------------|---------------------|
| Free      | 100        | 0 лв.               |
| Starter   | 1 000      | 19–29 лв.           |
| Business  | 10 000     | 79–99 лв.           |
| Enterprise| по договаряне | по оферта        |

- Годишен абонамент: 10–15% отстъпка (опционално).  
- За Enterprise – индивидуални лимити, SLA, договор.

---

## 7. Резюме – какво ми е нужно

1. **Решения от вас:**  
   - Само безплатен план в началото или и платени от ден 1?  
   - Плащане само по банков път (ръчно) или Stripe/PayPal (автоматично)?  
   - Примерни цени за всеки план.

2. **Технически:**  
   - Таблица за API ключове и (по избор) потребление.  
   - Endpoint за проверка с автентикация по ключ и rate limit.  
   - Страница „За разработчици“ с документация и форма за ключ.

3. **Правно:**  
   - Текст в Условията за ползване за API (лимити, отговорност, прекратяване).

4. **По избор за по-късно:**  
   - Stripe/PayPal за абонамент;  
   - личен кабинет за разработчици;  
   - допълнителни endpoints (статистики, списък).

След като изберете вариант (само Free / Free + платени с ръчно плащане / с автоматично разплащане), следваща стъпка е да се опише конкретната имплементация (ендпоинти, полета в БД, форма за ключ) в отделен технически документ или директно в задачи в UPDATE-V4.
