# Deployment Guide за Vercel

## Бърз старт

### Вариант 1: Чрез Vercel Dashboard (Препоръчителен)

1. **Създай акаунт** на [vercel.com](https://vercel.com)
2. **Свържи Git репозитория** (GitHub/GitLab/Bitbucket) или качи проекта директно
3. **Vercel автоматично разпознава** Next.js проекта
4. **Кликни "Deploy"** - готово! 🚀

### Вариант 2: Чрез Vercel CLI

```bash
# Инсталирай Vercel CLI
npm i -g vercel

# Влез в акаунта си
vercel login

# Deploy в production
vercel --prod
```

## Конфигурация

Проектът е конфигуриран с `vercel.json`:

- ✅ **Framework**: Next.js (автоматично разпознат)
- ✅ **API Functions**: Максимално време 30 секунди
- ✅ **Memory**: 1024 MB за API функции
- ✅ **Security Headers**: XSS защита, frame options, и др.
- ✅ **Region**: iad1 (US East)

## Важни бележки

### Файлове в `data/` папката

Файловете в `data/` папката **трябва да бъдат включени** в Git репозитория, за да бъдат достъпни след deployment.

Провери `.gitignore` - `data/` папката **НЕ трябва** да е игнорирана:

```bash
# В .gitignore НЕ трябва да има:
# /data/
# data/
```

### API Routes

API routes (`/api/debt`, `/api/debt/history`) работят като serverless функции:
- Автоматично мащабиране
- Pay-per-use модел
- Автоматично HTTPS

### Build процес

Vercel автоматично:
1. Инсталира зависимостите (`npm install`)
2. Build-ва проекта (`npm run build`)
3. Deploy-ва в production

### Environment Variables

Ако в бъдеще са нужни environment variables:
1. Vercel Dashboard → Project Settings → Environment Variables
2. Добави променливите
3. Redeploy проекта

## Проверка след Deployment

### 1. Проверка на API endpoints

```bash
# Текущи данни
curl https://your-project.vercel.app/api/debt

# Исторически данни
curl https://your-project.vercel.app/api/debt/history
```

### 2. Проверка на логове

Vercel Dashboard → Project → Functions → View Logs

### 3. Проверка на производителността

Vercel Dashboard → Analytics → Performance

## Обновяване на данните

### Автоматично (ако е свързан с Git)

1. Обнови `data/monthly-debt-data.json` локално
2. Commit и push промените
3. Vercel автоматично ще направи нов deployment

### Ръчно

1. Качи нови файлове в `data/` папката
2. Обнови `data/monthly-debt-data.json`
3. Push промените в Git
4. Vercel ще направи нов deployment

## Troubleshooting

### Проблем: API routes връщат грешки

**Решение:**
- Провери логовете в Vercel Dashboard
- Уверявай се, че `data/` папката е включена в Git
- Провери дали файловете са правилно форматирани

### Проблем: Файловете от `data/` не се зареждат

**Решение:**
- Провери `.gitignore` - `data/` не трябва да е игнорирана
- Уверявай се, че файловете са commit-нати в Git
- Провери пътищата в `app/api/debt/history/route.ts`

### Проблем: Build fail-ва

**Решение:**
- Провери `package.json` за правилни зависимости
- Провери TypeScript грешки: `npm run build` локално
- Провери Vercel build logs

## Production Checklist

- [ ] Проектът build-ва успешно локално (`npm run build`)
- [ ] Всички тестове минават (ако има)
- [ ] `data/` папката е включена в Git
- [ ] Environment variables са конфигурирани (ако са нужни)
- [ ] Custom domain е конфигуриран (ако е нужно)
- [ ] Analytics са активирани (ако е нужно)

## Допълнителни ресурси

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js on Vercel](https://vercel.com/docs/frameworks/nextjs)
- [Vercel CLI Reference](https://vercel.com/docs/cli)

